# Port the adapter listens on. Defaults to 8789.
PORT=8789

# In a REAL adapter you would put your upstream runtime credentials here, e.g.:
# OPENCLAW_API_KEY=...
# OPENCLAW_AGENT_ID=...
# This mock adapter needs NONE of these — it returns canned responses.
