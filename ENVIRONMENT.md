# Environment Configuration

## Development

VITE_BETA_URL=http://localhost:3000

## Production

Set `VITE_BETA_URL` to the deployed beta app origin when it is live.

Example preview value:

```env
VITE_BETA_URL=https://beta.usevoxa.vercel.app
```
